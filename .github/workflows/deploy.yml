name: Deploy to Oracle Cloud

on:
  push:
    branches: [ main ] # Срабатывает при каждом пуше в ветку main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            # Переходим в папку проекта
            cd ~/my-app
            # Забираем свежий код из GitHub
            git pull origin main
            # Пересобираем образ и перезапускаем контейнер
            sudo docker build -t my-custom-app .
            sudo docker rm -f my-real-app || true
            sudo docker run -d --name my-real-app -p 80:80 my-custom-app
